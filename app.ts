enum StatusCode {
  SUCCESS,
  IN_PROGRESS,
  FAILED,
}

const res = {
  message: "Payment  success",
  statusCode: StatusCode.SUCCESS,
};

function compute() {
  return 3;
}

const enum Roles {
  ADMIN = 1,
  USER = 2,
}

const res2 = Roles.ADMIN;
